# Docker容器如何走代理

## 目标
让容器内应用可通过指定代理访问外网。

## 步骤
1. 在运行参数或 compose 中注入 `HTTP_PROXY` / `HTTPS_PROXY` / `NO_PROXY`。
2. 进入容器验证环境变量。
3. 通过 `curl` 验证目标站点连通。
4. 如失败，检查宿主机到代理服务是否可达。

## 验证
- 容器内解析与访问都成功。
- 日志可见请求命中代理。
